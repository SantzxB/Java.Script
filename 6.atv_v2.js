// Sexta Atividade JS com retorno

const number = (a) =>{
    if (a > 0)
        resultado = (`Positivo!`)
    else
        resultado = (`Negativo!`)
    return resultado
}
const num = number(2)
console.log(`${num}`)